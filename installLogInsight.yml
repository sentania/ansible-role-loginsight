---
- hosts: all
  become: yes
  tasks:
  - name: Install log insight
    include: tasks/main.yml

  - name: Create a temporary file
    file:
      dest: "/tmp/{{ serviceLevel }}"
      owner: root
      group: root
      mode: '0644'
      state: touch
      